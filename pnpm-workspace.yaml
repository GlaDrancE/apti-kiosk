packages:
  - "apps/*"
  - "packages/*"

# Prisma and esbuild need their postinstall scripts to fetch engines/binaries.
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  prisma: true
